node_modules/
dist/
*.tsbuildinfo
*.tmp

# Local scraper state — not committed
cursor.json

# Local design plans — kept out of the repo (all plans live here now)
plans/

# Local secret — Alchemy RPC URL (never commit)
.rpc.env

# Privacy Pools measurement scratch
privacy-pools-config.json
pp-chunks/
pp-cache/
measurements/
pp-measure.sh
pp-measure.log

# Local agent instructions — not part of the public repo
CLAUDE.md

# Orchestrator lockfiles (publish.sh)
.locks/

privacy-pools-all-config.json
pp-all-chunks/
tornado-cash-config.json
